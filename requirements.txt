# BriskMoE dependencies
#
# Tested environment: Python 3.10, CUDA 12.8, RTX A6000 48 GB
#
# Install vllm first (it pulls torch, transformers, triton, etc.):
#   pip install vllm==0.16.0
#
# Then install the remaining packages:
#   pip install -r requirements.txt

# --- Core runtime ---
vllm==0.16.0
torch>=2.9.0
speculators>=0.4.0
numpy>=2.2

# --- Plotting (benchmark visualization) ---
matplotlib>=3.8

# --- Testing ---
pytest>=7.0

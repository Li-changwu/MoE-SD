policy_name: memory_partition_v2
controller: dynamic_memory_partition

update_interval_steps: 8
pressure_medium_ratio: 0.8
pressure_high_ratio: 0.9
smooth_alpha: 0.5

rules:
  - medium/high memory pressure increases kv reserve
  - low acceptance shrinks speculative budget
  - budgets are smoothed to avoid oscillation

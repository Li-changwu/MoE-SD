policy_name: prefetch_v1
controller: acceptance_aware_prefetch

hard_threshold: 0.60
soft_threshold: 0.35
io_cost_weight: 1.0

score_formula: p_expert_needed * p_token_accepted * benefit - cost

output_contract:
  hard: [expert_ids]
  soft: [expert_ids]
  defer: [expert_ids]
  reason: string
